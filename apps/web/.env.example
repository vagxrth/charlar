# Full URL of the backend (Express + Socket.IO).
# Inlined into the client bundle at build time.
# Dev: http://localhost:3001 | Prod: https://your-app.up.railway.app
NEXT_PUBLIC_SERVER_URL=http://localhost:3001
